#include <stdio.h>

int main() {
    // Exibe uma mensagem simples
    printf("Hello, World!\n");

    // Teste com variáveis
    int a = 5, b = 3;
    int soma = a + b;

    // Exibe o resultado da soma
    printf("A soma de %d e %d é: %d\n", a, b, soma);

    return 0;
}
